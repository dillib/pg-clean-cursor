node_modules
dist
test-results/
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Claude Code local state (agent worktrees, scheduled tasks, etc.)
.claude/

# Environment / secrets — never commit real keys
.env
.env.*
!.env.example
scripts/fly-secrets.*.env
!scripts/fly-secrets.env.example